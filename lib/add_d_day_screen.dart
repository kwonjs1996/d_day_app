import 'package:flutter/material.dart';

class AddDDayScreen extends StatelessWidget {
  const AddDDayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Add-D-day"),
    );
  }
}
